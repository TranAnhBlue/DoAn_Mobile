// Compatibility entry for Metro sessions that still reference the pre-move role path.
// The shared implementation remains in features/land-plots.
export { default } from '../../../features/land-plots/screens/LandPlotsScreen.js';
